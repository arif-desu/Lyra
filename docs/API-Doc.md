# Lyra API Documentation


## GPIO

GPIO is controlled with the following API calls:

- GPIO_Init()
```c 
int GPIO_Init(  uint16_t GPIOx, 
                uint16_t Pin, 
                uint16_t Direction)
```